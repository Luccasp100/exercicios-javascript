let A;
let B;
let C;
let soma_quadrados;
A = parseInt(prompt("Digite o valor de A:"));
B = parseInt(prompt("Digite o valor de B:"));
C = parseInt(prompt("Digite o valor de C:"));
soma_quadrados = (A * A) + (B * B) + (C * C);
alert("A soma dos quadrados de A, B e C é: " + soma_quadrados);
